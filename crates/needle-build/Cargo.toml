[package]
name = "needle-build"
description = "Needle 3 build: checkpoint -> .cact export (LoRA merge, ladder slicing, CQ quant)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
needle-format = { path = "../needle-format" }
serde_json = "1"
half = "2"
needle-tokenizer = { path = "../needle-tokenizer" }
needle-model = { path = "../needle-model" }
needle-engine = { path = "../needle-engine" }
